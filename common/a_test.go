package common
import (
	"fmt"
	// "strconv"
	"testing"
	"time"
)

func TestLevelRecuri(t *testing.T) {
	
	// var times time.Time
	a:=time.Now().Format("2006-01-02 15:04:05")
	fmt.Println(a)
	// fmt.Println(time.Now().String())
	// fmt.Println(time.Now().UnixMilli())
	// fmt.Println(string(time.Now().UnixMilli()))
	// fmt.Println(strconv.Itoa(time.Now().Nanosecond()))
	// timeMap:=make(map[string]struct{},1000)

	// for i := 0; i < 10000; i++ {
	// 	if _,ok:=timeMap[strconv.Itoa(time.Now().Nanosecond())];!ok{

	// 	}
	// }
	
}